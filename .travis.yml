language: node_js
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
script: npm test
before_deploy: npm i now --no-save
deploy:
  provider: script
  script: now --token $NOW_TOKEN && now alias
  skip_cleanup: true
  on:
    master: true